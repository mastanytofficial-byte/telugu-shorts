name: Apply narration prompt update v3

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, 'narration prompt recount')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Apply patch and validate
        run: |
          git apply --recount .github/narration-prompt.patch
          node --check index.js
          rm -f .github/narration-prompt.patch
          rm -f .github/workflows/apply-narration-prompt.yml
          rm -f .github/workflows/apply-narration-prompt-v2.yml
          rm -f .github/workflows/apply-narration-prompt-v3.yml
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.js .github
          git commit -m "chore: improve Telugu Shorts narration prompts"
          git push
